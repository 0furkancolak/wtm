{
  "name": "worktree-runtime-manager",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.0",
    "node": ">=24.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit --project packages/protocol/tsconfig.json && tsc --noEmit --project packages/core/tsconfig.json && tsc --noEmit --project packages/testkit/tsconfig.json",
    "build": "bun build packages/protocol/src/index.ts --outdir dist/protocol --target node && bun build packages/core/src/index.ts --outdir dist/core --target node --external better-sqlite3 && mkdir -p dist/core/migrations && cp packages/core/src/state/migrations/001-initial.sql dist/core/migrations/001-initial.sql"
  },
  "devDependencies": {
    "@types/better-sqlite3": "9.6.0",
    "@types/bun": "^1.3.0",
    "node-gyp": "13.0.1",
    "tsx": "4.23.12",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "better-sqlite3": "13.0.0",
    "smol-toml": "1.8.0",
    "zod": "^4.1.0"
  }
}
