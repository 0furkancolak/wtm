{
  "name": "worktree-runtime-manager",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.0",
    "node": ">=24.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit --project packages/protocol/tsconfig.json && tsc --noEmit --project packages/core/tsconfig.json && tsc --noEmit --project packages/testkit/tsconfig.json",
    "build": "bun build packages/protocol/src/index.ts --outdir dist/protocol --target node && bun build packages/core/src/index.ts --outdir dist/core --target node"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "zod": "^4.1.0"
  }
}
